# XroidVerse Workflow

- Use `develop` as the primary integration branch.
- Direct commits and pushes to `develop` are permitted.
- When using feature branches, they can be merged directly into `develop` without a pull request.
- Always check whether changes require updates under `Samples/`, and update them when needed.