# Changelog

## Unreleased

- Require `task-tracker` writes that modify `.tasks.jsonl` in `mwt` repos to
  run from the owning task worktree instead of the seed checkout.
- Require agents to enumerate changed externally visible contract surfaces and
  map each changed surface item to direct verification evidence before claiming
  completion.
- Require bug fixes to add a mechanical contract check or matrix when the
  escaped defect shows that a changed user-visible surface item lacked direct
  verification.
